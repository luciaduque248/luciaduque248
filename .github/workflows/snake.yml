name: Generate contribution snake

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: "15 0 * * *"

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Generate snake SVGs
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#FF6B9A&color_dots=#EBEDF0,#FFD1E0,#FFB3CB,#FF87AC,#FF6B9A
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#00C2A8&color_dots=#161B22,#123D3A,#0A6D62,#00A891,#00C2A8

      - name: Publish snake to output branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: output
          publish_dir: ./dist
